/*************************************************************************************************
 SCOPE_IDENTITY()  
 - INSERT한 마지막 행의 키값 반환
**************************************************************************************************/
DECLARE @ID INT
INSERT INTO dbo.TBLIDentity(IDName)
SELECT N'하박사'

SELECT @ID = SCOPE_IDENTITY()  

SELECT @ID
